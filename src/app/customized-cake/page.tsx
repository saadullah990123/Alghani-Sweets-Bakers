import CustomizedCakesPage, { metadata as customizedCakesMetadata } from '../customized-cakes/page';

export const metadata = customizedCakesMetadata;
export const dynamic = 'force-dynamic';
export const revalidate = 0;

export default CustomizedCakesPage;
